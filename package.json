{
  "devDependencies": {
    "cross-env": "^10.1.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:frontend": "cross-env PORT=3000 npm --prefix frontend run dev"
  }
}
